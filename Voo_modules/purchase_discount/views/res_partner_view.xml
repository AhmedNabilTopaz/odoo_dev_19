<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_partner_form_view_purchase_discount" model="ir.ui.view">
        <field name="name">purchase.discount.res.partner.form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='property_product_pricelist']" position="after">
                <field name="default_supplierinfo_discount"
                    invisible="not is_company and parent_id"/>
                <field name="default_supplierinfo_discount"
                    invisible="is_company or not parent_id"/>
            </xpath>
        </field>
    </record>
</odoo>
